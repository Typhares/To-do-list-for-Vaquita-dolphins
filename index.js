'use strict';


$("li").click(function(){
    if li is gray
        turn it back
    else if 
        turn back
    $(this).css({
        color: "gray",
        textDecoration: "line-through"
    })
});
